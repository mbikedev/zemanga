[build]
  command = "npx @netlify/plugin-nextjs@latest"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Environment variables (set via Netlify CLI or UI)
# SMTP_HOST
# SMTP_PORT
# SMTP_USER
# SMTP_PASSWORD
